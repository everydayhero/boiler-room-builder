import assert from 'assert'

describe('Running tests', () => {
  it('runs tests', () => {
    const x = 1
    assert(x === 1)
  })

  it('totes runs tests', () => {
    const y = 1
    assert(y === 1)
  })
})
